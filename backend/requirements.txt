# G20 Demo Backend Requirements
# Target: Windows (dev) + Jetson Orin NX (production)

# Core
numpy>=1.26
scipy>=1.12

# PyTorch - CUDA 11.8 (works on both Windows and Jetson)
torch>=2.1.0
torchvision>=0.16.0

# gRPC
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# WebSocket (for Flutter client)
websockets>=12.0

# Optional: TensorRT (auto-detected)
# Install via NVIDIA: pip install tensorrt
# On Jetson: pre-installed

# Optional: ONNX Runtime (fallback)
# onnxruntime-gpu>=1.16.0

# Configuration (Phase 4 - Pydantic-based config)
pydantic>=2.5
pydantic-settings>=2.1

# Testing (Phase 5 - Testing Sprint)
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
hypothesis>=6.100

# Utilities
psutil>=5.9
